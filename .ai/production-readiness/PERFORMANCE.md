# Performance

No performance claim is made. Initial static public assets total 7.42 MB; tutorial screenshots account for most of it. Next image optimization negotiates AVIF/WebP for configured Supabase images (`next.config.ts:13-23`). Route bundles, Web Vitals, server/database latency, query counts and constrained-network behavior were not measured because the production build fails.
